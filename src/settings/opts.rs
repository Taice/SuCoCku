pub struct Opts {
    pub highlight_box: bool,
    pub highlight_in_line: bool,
    pub highlight_cell: bool,
    pub command_font_size: u16,
    pub visual_highlight_size: f32,
    pub auto_candidate_elimination: bool,
}
